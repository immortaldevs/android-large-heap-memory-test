android-large-heap-memory-test
==============================

This is a simple app that will display the suggested heap size and max heap size for the device when
the the manifest flag for a large heap (android:largeHeap="true") is set.
